name: "GPU Tests"

on:
  pull_request:
    branches:
      - master
      - 'release-'
    paths-ignore:
      - 'docs/**'
  push:
    branches:
      - master
    paths-ignore:
      - 'docs/**'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref_name != github.event.repository.default_branch || github.ref != 'refs/tags/v*' }}

jobs:
  # CUDA tests run via the grouped-tests matrix (test/test_groups.toml `[CUDA]`
  # group, self-hosted gpu-v100 runner) defined in Tests.yml. This workflow now
  # only builds the GPU documentation.
  gpu-docs:
    name: "Documentation"
    runs-on: [self-hosted, gpu-v100]
    timeout-minutes: 360
    if: github.event_name == 'push' || !github.event.pull_request.draft
    env:
      # The runner is a V100 (compute capability 7.0). Reactant_jll picks its CUDA
      # build from the driver version (13.x here), but CUDA 13 dropped pre-Turing
      # kernels, so XLA's GEMM autotuner finds no compilable config and the
      # Reactant example dies with "No candidates could be compiled". Pin the
      # CUDA 12.9 build, the last one with Volta support.
      REACTANT_GPU_VERSION: "12.9"
    steps:
      - uses: actions/checkout@v7
      - uses: julia-actions/setup-julia@v3
        with:
          version: "1"
      - name: Instantiating project
        shell: julia --color=yes --project=docs {0}
        run: |
          using Pkg
          Pkg.develop(PackageSpec(path=pwd()))
          Pkg.instantiate()
      - name: Building documentation
        run: julia --color=yes --project=docs docs/make.jl
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DOCUMENTER_KEY: ${{ secrets.DOCUMENTER_KEY }}
          DATADEPS_ALWAYS_ACCEPT: true
